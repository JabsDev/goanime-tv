---
phase: 03-error-handling-bug-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/core/sources/anime_fire_adapter.dart
  - lib/core/sources/goyabu_adapter.dart
  - lib/core/sources/super_flix_adapter.dart
  - lib/core/sources/all_anime_adapter.dart
  - lib/features/superflix/superflix_web_screen.dart
autonomous: true
requirements:
  - CODE-03
  - BUG-01
must_haves:
  truths:
    - "Unparseable episode numbers sort to end of episode list (not position 0)"
    - "OVA/Special episodes no longer break episode ordering"
    - "SuperFlix WebView uses AppConstants.superFlixBase as domain source of truth"
    - "All existing search, episode listing, and playback flows continue to work unchanged"
    - "No interface changes — adapters still return List<Anime>, EpisodesResult, List<VideoSource>"
  artifacts:
    - path: "lib/core/sources/anime_fire_adapter.dart"
      provides: "Episode sort sentinel fix in getEpisodes"
      contains: "double.infinity.toInt() within sort comparator"
    - path: "lib/core/sources/goyabu_adapter.dart"
      provides: "Episode sort sentinel fix in getEpisodes"
      contains: "double.infinity.toInt() within sort comparator"
    - path: "lib/core/sources/super_flix_adapter.dart"
      provides: "Episode sort sentinel fix at FFI and HTTP sort call sites"
      contains: "double.infinity within sort comparator"
    - path: "lib/core/sources/all_anime_adapter.dart"
      provides: "Episode sort sentinel fix in getEpisodes"
      contains: "double.infinity within sort comparator"
    - path: "lib/features/superflix/superflix_web_screen.dart"
      provides: "SuperFlix domain from AppConstants.superFlixBase instead of hardcoded .pro"
      contains: "AppConstants.superFlixBase"
  key_links:
    - from: "superflix_web_screen.dart"
      to: "app_constants.dart"
      via: "AppConstants.superFlixBase reference"
      pattern: 'AppConstants\.superFlixBase'
    - from: "anime_fire_adapter.dart line 89"
      to: "episode sort comparator"
      via: "sentinel replacement"
      pattern: 'double\.infinity\.toInt\(\)'
    - from: "goyabu_adapter.dart lines 157-158"
      to: "episode sort comparator"
      via: "sentinel replacement"
      pattern: 'double\.infinity\.toInt\(\)'
    - from: "super_flix_adapter.dart lines 139, 190"
      to: "episode sort comparator"
      via: "sentinel replacement"
      pattern: 'double\.infinity'
    - from: "all_anime_adapter.dart line 155"
      to: "episode sort comparator"
      via: "sentinel replacement"
      pattern: 'double\.infinity'
---

<objective>
Fix the episode sorting sentinel bug (BUG-01) and the SuperFlix domain inconsistency (CODE-03).

**Purpose:** Two standalone bug fixes that don't require interface changes. Episode sorting is repaired by replacing the `?? 0` fallback with `double.infinity` / `double.infinity.toInt()` so unparseable episode numbers (OVA/Special/specials) sort to the end of the list. The SuperFlix domain is unified by replacing the hardcoded `'https://superflixapi.pro'` in the WebView with `AppConstants.superFlixBase`.

**Output:** Modified adapters with correct episode sorting, modified WebView with correct domain.
</objective>

<execution_context>
@/home/jabs/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/jabs/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-error-handling-bug-fixes/03-CONTEXT.md
@.planning/phases/03-error-handling-bug-fixes/03-RESEARCH.md

# Source files being modified — read each before editing
@lib/core/sources/anime_fire_adapter.dart
@lib/core/sources/goyabu_adapter.dart
@lib/core/sources/super_flix_adapter.dart
@lib/core/sources/all_anime_adapter.dart
@lib/features/superflix/superflix_web_screen.dart
@lib/core/constants/app_constants.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix episode sort sentinel at all 5 sort call sites</name>
  <files>
    lib/core/sources/anime_fire_adapter.dart
    lib/core/sources/goyabu_adapter.dart
    lib/core/sources/super_flix_adapter.dart
    lib/core/sources/all_anime_adapter.dart
  </files>
  <read_first>
    lib/core/sources/anime_fire_adapter.dart (line 89-91)
    lib/core/sources/goyabu_adapter.dart (lines 157-159)
    lib/core/sources/super_flix_adapter.dart (lines 138-140, 189-191)
    lib/core/sources/all_anime_adapter.dart (line 155)
  </read_first>
  <action>
    Per D-11: Replace the `?? 0` sort sentinel with `double.infinity` or `double.infinity.toInt()` at all 5 call sites. Standardize on `double.infinity` as the single sentinel value per RESEARCH.md Pitfall 2 guidance.

    1. **AnimeFire** (line 89, int-based): Change `int.tryParse(a.number) ?? 0` → `int.tryParse(a.number) ?? double.infinity.toInt()`. Same for `b.number`.

    2. **Goyabu** (lines 157-158, int-based): The comparator uses `int.tryParse(RegExp(r'\d+').firstMatch(a.number)?.group(0) ?? '0') ?? 0`. Change the trailing `?? 0` to `?? double.infinity.toInt()`. Same for `b.number`.

    3. **SuperFlix** (line 139, double-based — FFI path): Change `(double.tryParse(a.number) ?? 0)` → `(double.tryParse(a.number) ?? double.infinity)`. Same for `b.number`.

    4. **SuperFlix** (line 190, double-based — HTTP path): Same change as #3.

    5. **AllAnime** (line 155, double-based): Change `(double.tryParse(a) ?? 0)` → `(double.tryParse(a) ?? double.infinity)`. Same for `b`.

    Do NOT change any other logic, whitespace, or imports. These are single-value sentinel replacements only.
  </action>
  <verify>
    <automated>grep -c 'double\.infinity' lib/core/sources/anime_fire_adapter.dart lib/core/sources/goyabu_adapter.dart lib/core/sources/super_flix_adapter.dart lib/core/sources/all_anime_adapter.dart</automated>
  </verify>
  <acceptance_criteria>
    - anime_fire_adapter.dart: sort comparator contains `double.infinity.toInt()` instead of `?? 0`
    - goyabu_adapter.dart: sort comparator contains `double.infinity.toInt()` instead of `?? 0`
    - super_flix_adapter.dart: both sort comparators (FFI path line ~139, HTTP path line ~190) contain `double.infinity`
    - all_anime_adapter.dart: sort comparator contains `double.infinity`
    - No `?? 0` remains in any of the 4 adapter files' sort sites (grep for `?? 0` in these files should find zero matches in sort context)
    - `flutter analyze` passes
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Fix SuperFlix WebView domain to use AppConstants.superFlixBase</name>
  <files>
    lib/features/superflix/superflix_web_screen.dart
  </files>
  <read_first>
    lib/features/superflix/superflix_web_screen.dart (lines 64-68, plus imports at top)
    lib/core/constants/app_constants.dart
  </read_first>
  <action>
    Per D-10: Replace the hardcoded `'https://superflixapi.pro'` in `_playerUrl` with `AppConstants.superFlixBase`.

    Exact changes in superflix_web_screen.dart:
    1. **Add import**: Add `import '../../core/constants/app_constants.dart';` to the imports block (after the theme_constants import on line 5).
    2. **Replace return value** at line 67: Change `return 'https://superflixapi.pro/serie/$_tmdbId/$season/${widget.episode.number}';` to `return '${AppConstants.superFlixBase}/serie/$_tmdbId/$season/${widget.episode.number}';`

    No other changes to this file. Do not touch the WebView JavaScript, the extraction logic, the polling, or any other UI code.
  </action>
  <verify>
    <automated>grep -c 'superflixapi\.pro' lib/features/superflix/superflix_web_screen.dart</automated>
  </verify>
  <acceptance_criteria>
    - superflix_web_screen.dart imports `app_constants.dart`
    - `_playerUrl` getter uses `AppConstants.superFlixBase` string interpolation
    - No hardcoded `superflixapi.pro` remains in the file (grep returns 0 matches)
    - `flutter analyze` passes
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| adapter→provider HTTP | External provider response contains untrusted HTML/JSON |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | Episode sort sentinel (all 4 adapters) | mitigate | Sentinel uses `double.infinity` — a well-defined Dart constant — not a magic value that could overflow or sort inconsistently |
</threat_model>

<verification>
- `flutter analyze` passes with zero errors/warnings (verifies all adapter files and WebView are syntactically correct)
- Search for any remaining `superflixapi.pro` in the codebase — must find 0 occurrences
- Search for any remaining `?? 0` in sort comparators across all 4 adapters — must find 0 occurrences
</verification>

<success_criteria>
1. All 5 episode sort call sites use `double.infinity` or `double.infinity.toInt()` as sentinel — verified by grep
2. superflix_web_screen.dart contains `AppConstants.superFlixBase` and zero occurrences of hardcoded `.pro`
3. `flutter analyze` passes
4. No regression in adapter method signatures or return types (no changes to interface)
</success_criteria>

<output>
Create `.planning/phases/03-error-handling-bug-fixes/03-01-SUMMARY.md` when done
</output>
