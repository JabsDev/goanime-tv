---
phase: 04-testing-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pubspec.yaml
  - test/core/cache/ttl_cache_test.dart
  - test/core/utils/text_utils_test.dart
  - test/data/models/anilist_models_test.dart
autonomous: true
requirements: [TEST-01]
user_setup: []

must_haves:
  truths:
    - "D-01: mocktail ^1.0.5 is available as a dev dependency"
    - "D-07: Test directory mirrors lib/ structure (test/core/cache/, test/core/utils/, test/data/models/, etc.)"
    - "Developer can run 'flutter test test/' and get meaningful results"
    - "TtlCache get/set/contains/remove/clear/eviction tests all pass"
    - "TextUtils cleanTitle/treatName/extractSuperFlixSeason tests all pass"
    - "AniListMediaDetail.fromJson deserialization tests all pass"
  artifacts:
    - path: "pubspec.yaml"
      provides: "mocktail dev dependency"
      contains: "mocktail"
    - path: "test/core/cache/ttl_cache_test.dart"
      provides: "TtlCache unit tests"
      min_lines: 40
    - path: "test/core/utils/text_utils_test.dart"
      provides: "TextUtils unit tests"
      min_lines: 40
    - path: "test/data/models/anilist_models_test.dart"
      provides: "AniList model deserialization tests"
      min_lines: 50
  key_links:
    - from: "test/core/cache/ttl_cache_test.dart"
      to: "lib/core/cache/ttl_cache.dart"
      via: "import TtlCache"
    - from: "test/core/utils/text_utils_test.dart"
      to: "lib/core/utils/text_utils.dart"
      via: "import TextUtils"
    - from: "test/data/models/anilist_models_test.dart"
      to: "lib/data/models/anilist_models.dart"
      via: "import AniListMediaDetail"

---

## Phase Goal

**As a** developer, **I want to** run automated tests that verify core logic, scraper behavior, and adapter parsing, **so that** I can detect regressions during development without manual testing.

## Multi-Source Coverage Audit

### GOAL: "Establish test coverage and developer documentation"

| Goal Sub-item | Plan(s) | Status |
|---------------|---------|--------|
| Unit tests for core logic (TtlCache, TextUtils, AniList models) | 04-01 | ✓ Covered |
| Unit tests for scraper orchestration (_bestMatch, _normalize, ScraperResult, isCloudflareChallenge) | 04-02 | ✓ Covered |
| Integration test (search → detail → playback) | 04-03 | ✓ Covered |
| Developer documentation (AGENTS.md) | 04-03 | ✓ Covered |

### REQ: Phase Requirement IDs

| ID | Description | Plan(s) |
|----|-------------|---------|
| CODE-04 | Add AGENTS.md with workflow and build instructions | 04-03 |
| TEST-01 | Add unit tests for core scraper and adapter logic | 04-01, 04-02 |
| TEST-02 | Expand integration test coverage | 04-03 |

### RESEARCH: Technical Findings

| Feature | Plan(s) | Status |
|---------|---------|--------|
| mocktail as mocking library (Standard Stack) | 04-01 | ✓ Covered |
| Pure unit test patterns (TtlCache + fake_async, TextUtils) | 04-01 | ✓ Covered |
| AniListMediaDetail.fromJson fixture tests | 04-01 | ✓ Covered |
| _bestMatch/_normalize package-visible recommendation | 04-02 | ✓ Covered |
| isCloudflareChallenge content/header detection tests | 04-02 | ✓ Covered |
| ScraperResult sealed class pattern matching tests | 04-02 | ✓ Covered |
| Integration test following scraper_smoke_test.dart pattern | 04-03 | ✓ Covered |
| Fixture-based adapter parsing tests (D-06 priority) | — | ⚠ Deferred — see Exclusions |

### CONTEXT (D-XX Decisions)

| Decision | Plan(s) | Status |
|----------|---------|--------|
| D-01: mocktail as mocking library | 04-01 | ✓ Covered |
| D-02: HTML fixture files for adapter tests | — | ⚠ Deferred — see Exclusions |
| D-03: Test fixtures in test/fixtures/{adapter_name}/ | — | ⚠ Deferred (depends on D-06) |
| D-04: Core utilities first (Priority 1) | 04-01 | ✓ Covered |
| D-05: Scraper orchestration (Priority 2) | 04-02 | ✓ Covered |
| D-06: Adapter parsing with fixture files (Priority 3) | — | ⚠ Deferred — see Exclusions |
| D-07: Test directory mirrors lib/ structure | 04-01, 04-02 | ✓ Covered |
| D-08: One new integration test | 04-03 | ✓ Covered |
| D-09: Keep existing 4 integration tests untouched | All | ✓ Constraint enforced |
| D-10: Concise AGENTS.md format (~50-80 lines) | 04-03 | ✓ Covered |
| D-11: AGENTS.md sections (Build, Structure, Testing) | 04-03 | ✓ Covered |

### ⚠ Exclusions (not gaps)

| Item | Reason |
|------|--------|
| D-06: Adapter parsing fixture tests | Requires production code refactoring (optional `http.Client?` constructor parameter to 4 adapter files). This is a design concern outside the scope of a pure testing phase — adapting the adapter constructors touches production architecture, not just test infrastructure. Recommended as either a combined design+test sub-phase or deferred to a code-cleanup phase. |
| Coverage tooling (`flutter test --coverage`, Codecov) | Explicitly deferred in CONTEXT.md Deferred Ideas section |
| CI Pipeline (GitHub Actions) | Explicitly deferred to Phase 5 (Release Configuration) |

<objective>
**What:** Set up the test infrastructure and write the first batch of pure-logic unit tests.

**Purpose:** Establish the test directory structure, add the mocktail mocking library, and create high-ROI unit tests for classes that require zero mocking (TtlCache, TextUtils, AniList model deserialization). After this plan, `flutter test test/` produces meaningful results.

**Output:**
- `pubspec.yaml` — mocktail dev dependency added
- `test/core/cache/ttl_cache_test.dart` — TtlCache unit tests
- `test/core/utils/text_utils_test.dart` — TextUtils unit tests
- `test/data/models/anilist_models_test.dart` — AniList model deserialization tests
- Directory structure: `test/core/cache/`, `test/core/utils/`, `test/core/scraper/`, `test/core/helpers/`, `test/data/models/`, `test/fixtures/`, `test/integration/`

</objective>

<execution_context>
@/home/jabs/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/jabs/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-testing-documentation/04-CONTEXT.md
@.planning/phases/04-testing-documentation/04-RESEARCH.md

<interfaces>
<!-- Key types and contracts from the codebase. No codebase exploration needed. -->

From lib/core/cache/ttl_cache.dart:
```dart
class TtlCache {
  final Duration defaultTtl;
  final int maxSize;
  final Map<String, _CacheEntry<dynamic>> _store = {};
  TtlCache({this.defaultTtl = const Duration(minutes: 10), this.maxSize = 300});
  T? get<T>(String key);
  void set<T>(String key, T value, {Duration? ttl});
  bool contains(String key);
  void remove(String key);
  void clear();
}
```

From lib/core/utils/text_utils.dart:
```dart
class TextUtils {
  const TextUtils._();
  static String treatName(String name);
  static String cleanTitle(String title);
  static String? extractSuperFlixSeason(String url, String? tmdbId);
}
```

From lib/data/models/anilist_models.dart:
```dart
class AniListMediaDetail {
  final int id;
  final String? bannerImage;
  final String? description;
  final int? episodes;
  final String? status;
  final double? averageScore;
  final List<String> genres;
  final AniListCoverImage coverImage;
  factory AniListMediaDetail.fromJson(Map<String, dynamic> json);
}
class AniListCoverImage {
  final String best;
  final String large;
  final String medium;
  final String extraLarge;
  factory AniListCoverImage.fromJson(Map<String, dynamic> json);
}
class AniListGraphQLResponse {
  final AniListGraphQLMediaData data;
  factory AniListGraphQLResponse.fromJson(Map<String, dynamic> json);
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add mocktail dev dependency + create test directory structure</name>
  <files>pubspec.yaml</files>
  <read_first>pubspec.yaml</read_first>
  <action>
    Add `mocktail: ^1.0.5` to `dev_dependencies` in pubspec.yaml (insert after `test: ^1.25.0` on the line after it, maintaining alphabetical grouping). Then run `flutter pub get` to resolve the dependency.

    After pub get succeeds, create the full test directory tree under `test/`:
    - `test/core/cache/`
    - `test/core/utils/`
    - `test/core/scraper/`
    - `test/core/helpers/`
    - `test/data/models/`
    - `test/fixtures/anime_fire/`
    - `test/fixtures/all_anime/`
    - `test/fixtures/super_flix/`
    - `test/fixtures/goyabu/`
    - `test/integration/`

    Leave the directories empty (`.gitkeep` is not needed — Flutter's test runner ignores empty dirs). Run `flutter pub get` to confirm mocktail resolves.
  </action>
  <verify>
    <automated>grep -c "mocktail:" pubspec.yaml && flutter pub get</automated>
  </verify>
  <acceptance_criteria>
    - `pubspec.yaml` contains `mocktail: ^1.0.5` under dev_dependencies
    - `flutter pub get` exits 0 with no errors
    - All directories under `test/` listed above exist and are empty (no test files yet)
  </acceptance_criteria>
  <done>pubspec.yaml has mocktail: ^1.0.5 in dev_dependencies; flutter pub get succeeds; test/ directory tree exists</done>
</task>

<task type="auto">
  <name>Task 2: Create ttl_cache_test.dart with fake_async TTL tests</name>
  <files>test/core/cache/ttl_cache_test.dart</files>
  <read_first>lib/core/cache/ttl_cache.dart</read_first>
  <action>
    Create `test/core/cache/ttl_cache_test.dart` with the following test groups and cases. Use `package:fake_async/fake_async.dart` for all time-dependent tests (TTL expiry, per-entry TTL). Use plain `test()` (not `testWidgets`) since TtlCache is pure Dart with no Flutter dependency.

    Import path: `package:goanime_tv/core/cache/ttl_cache.dart`

    Test cases:
    1. `get returns value when not expired` — set key, immediate get returns value
    2. `get returns null after TTL expires` — set key, elapse past TTL, get returns null
    3. `evicts oldest entry when at max capacity` — set 3 entries with maxSize=2, oldest is evicted
    4. `contains returns false for missing or expired keys` — missing key returns false, set then contains returns true
    5. `remove clears a single entry` — set two keys, remove one, verify one removed one kept
    6. `clear empties all entries` — set two keys, clear, both return null
    7. `per-entry TTL overrides default` — set short-TTL entry and long-TTL entry, elapse past short TTL, verify short expired and long valid
    8. `get refreshes recency on access` — set 2 entries with maxSize=2, access first, set third, verify LRU eviction keeps accessed entry

    Use `FakeAsync().run((async) { ... })` pattern for time-dependent tests. Use `DateTime.now()` based tests for eviction/remove/clear (wall-clock, no fake_async needed for those).
  </action>
  <verify>
    <automated>flutter test test/core/cache/ttl_cache_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `test/core/cache/ttl_cache_test.dart`
    - All 8 test cases pass: get/unexpired, get/expired, eviction, contains, remove, clear, per-entry TTL, recency refresh
    - Uses `FakeAsync` only for time-dependent tests; plain test() for eviction/remove/clear
    - `flutter test test/core/cache/ttl_cache_test.dart` exits 0
  </acceptance_criteria>
  <done>test/core/cache/ttl_cache_test.dart has 8 test cases covering all TtlCache methods; flutter test passes</done>
</task>

<task type="auto">
  <name>Task 3: Create text_utils_test.dart + anilist_models_test.dart</name>
  <files>
    test/core/utils/text_utils_test.dart,
    test/data/models/anilist_models_test.dart
  </files>
  <read_first>
    lib/core/utils/text_utils.dart,
    lib/data/models/anilist_models.dart
  </read_first>
  <action>
    Create two unit test files. Use plain `test()` (not `testWidgets`) for all tests since these are pure Dart with no Flutter dependency.

    **File 1: `test/core/utils/text_utils_test.dart`**

    Import: `package:goanime_tv/core/utils/text_utils.dart`

    Group `TextUtils.cleanTitle`:
    1. removes source tags `[AnimeFire]` and `[AllAnime]`
    2. removes sub/dub qualifiers (`Dublado`, `Legendado`, `Dub`, `Sub`)
    3. removes `todos os episodios` suffix
    4. strips season/episode number suffixes (`1.0 A1`, trailing digits)
    5. removes sub/dub in parentheses (`(Dublado)`, `(legendado)`)
    6. removes episode count in parentheses (`(25 episodes)`, `(64 eps)`)
    7. collapses multiple spaces and trims
    8. handles empty string

    Group `TextUtils.treatName`:
    1. lowercases and replaces spaces with hyphens
    2. handles single word
    3. handles empty string

    Group `TextUtils.extractSuperFlixSeason`:
    1. extracts season number from URL with matching tmdbId
    2. returns null when tmdbId is null
    3. returns null when URL does not match tmdbId

    **File 2: `test/data/models/anilist_models_test.dart`**

    Import: `package:goanime_tv/data/models/anilist_models.dart`

    Use the `_fullJson` fixture pattern — define private JSON maps within the test file matching AniList API response shapes.

    Group `AniListMediaDetail.fromJson`:
    1. parses full response correctly (id 16498, bannerImage, description, episodes 500, status FINISHED, averageScore 80.0, genres, coverImage all fields)
    2. handles null/empty fields gracefully (empty map → id 0, nulls, empty genres, coverImage.best '')
    3. handles missing coverImage (coverImage.best returns '')
    4. handles averageScore as int (converts to 75.0)
    5. handles averageScore as double (parses as 75.5)
    6. handles null averageScore (returns null)

    Group `AniListCoverImage.fromJson`:
    1. parses extraLarge as best when available
    2. falls back to large when extraLarge missing
    3. falls back to medium when only medium available
    4. returns empty string when no images

    Group `AniListGraphQLResponse.fromJson`:
    1. parses valid GraphQL response with nested Media
    2. handles missing data field
    3. handles null Media field
  </action>
  <verify>
    <automated>flutter test test/core/utils/text_utils_test.dart && flutter test test/data/models/anilist_models_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - text_utils_test.dart contains all cleanTitle (8 cases), treatName (3 cases), extractSuperFlixSeason (3 cases) tests — all pass
    - anilist_models_test.dart contains all AniListMediaDetail.fromJson (6 cases), AniListCoverImage.fromJson (4 cases), AniListGraphQLResponse.fromJson (3 cases) tests — all pass
    - `flutter test test/core/utils/text_utils_test.dart` exits 0
    - `flutter test test/data/models/anilist_models_test.dart` exits 0
  </acceptance_criteria>
  <done>text_utils_test.dart covers 14 cases (cleanTitle 8 + treatName 3 + extractSuperFlixSeason 3); anilist_models_test.dart covers 13 cases (MediaDetail 6 + CoverImage 4 + GraphQLResponse 3); both flutter test commands pass</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries introduced in this phase. Test infrastructure and unit tests run in the developer environment or CI. No network access, no user data, no production code changes.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | mocktail package install | mitigate | mocktail 1.0.5 verified via pub.dev API — established package by felangel, 15M+ downloads, 6 years on registry. See RESEARCH.md Package Legitimacy Audit. |
| T-04-SC | Tampering | Dart packages via pub | mitigate | Dependency pinned to `^1.0.5` — pub.dev verified registry. No [SLOP] or [SUS] packages. |
</threat_model>

<verification>
```bash
# Run all unit tests from this plan
flutter test test/core/cache/ttl_cache_test.dart
flutter test test/core/utils/text_utils_test.dart
flutter test test/data/models/anilist_models_test.dart
# Quick smoke that all unit tests pass together
flutter test test/
```
</verification>

<success_criteria>
- [x] `pubspec.yaml` has mocktail dev dependency
- [x] Test directory structure mirrors lib/
- [x] TtlCache unit tests: 8 cases covering all public methods + TTL + eviction
- [x] TextUtils unit tests: cleanTitle (8), treatName (3), extractSuperFlixSeason (3)
- [x] AniList model tests: AniListMediaDetail.fromJson (6), AniListCoverImage (4), AniListGraphQLResponse (3)
- [x] `flutter test test/core/cache/ttl_cache_test.dart` — all pass
- [x] `flutter test test/core/utils/text_utils_test.dart` — all pass
- [x] `flutter test test/data/models/anilist_models_test.dart` — all pass
</success_criteria>

<output>
Create `.planning/phases/04-testing-documentation/04-01-SUMMARY.md` when done
</output>
