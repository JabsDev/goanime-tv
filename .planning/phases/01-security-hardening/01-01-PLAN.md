---
phase: 01-security-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/security/pkce_pairing_test.dart
  - lib/core/anilist/anilist_pairing_server.dart
  - lib/core/constants/app_constants.dart
  - lib/core/anilist/anilist_service.dart
  - lib/core/storage/local_storage.dart
autonomous: true
requirements:
  - SEC-01
  - SEC-02
  - SEC-03

must_haves:
  truths:
    - "User can pair AniList account via phone scanning QR code on TV (PKCE flow)"
    - "AniList client secret is no longer hardcoded in app_constants.dart"
    - "/token endpoint on pairing server rejects requests without valid CSRF token"
    - "/token endpoint rejects requests with mismatched Origin/Referer headers"
    - "/token endpoint rate-limits excessive POSTs from the same IP (max 5/60s)"
    - "Access-Control-Allow-Origin is restricted to TV's own origin"
    - "AniListService stores tokens via LocalStorage (SharedPreferences wrapper)"
    - "Pairing page documents LAN-security boundary warning"
  artifacts:
    - path: "test/security/pkce_pairing_test.dart"
      provides: "End-to-end integration tests for PKCE + CSRF + rate limiting"
      min_lines: 80
      contains: "code_challenge_method=S256"
    - path: "lib/core/anilist/anilist_pairing_server.dart"
      provides: "PKCE-based OAuth pairing server with CSRF, origin validation, rate limiting"
      min_lines: 200
      exports: ["AniListPairingServer"]
    - path: "lib/core/constants/app_constants.dart"
      provides: "Configuration constants without anilistClientSecret"
      min_lines: 14
      not_contains: "anilistClientSecret"
    - path: "lib/core/anilist/anilist_service.dart"
      provides: "AniList OAuth service with PKCE exchange method + LocalStorage token storage"
      min_lines: 150
      exports: ["AniListService"]
    - path: "lib/core/storage/local_storage.dart"
      provides: "Token-specific helper methods (getToken, saveToken, removeToken)"
      min_lines: 120
      contains: "static Future<bool> saveToken"
  key_links:
    - from: "anilist_pairing_server.dart"
      to: "anilist_service.dart"
      via: "AniListService.exchangeCodeForToken()"
      pattern: "AniListService\\.exchangeCode"
    - from: "anilist_pairing_server.dart"
      to: "app_constants.dart"
      via: "AppConstants.anilistClientId"
      pattern: "AppConstants\\.anilistClientId"
      note: "must NOT reference anilistClientSecret"
    - from: "anilist_service.dart"
      to: "local_storage.dart"
      via: "LocalStorage.saveToken() / LocalStorage.getToken()"
      pattern: "LocalStorage\\.(save|get|remove)Token"
    - from: "anilist_service.dart"
      to: "home_screen.dart"
      via: "AniListService.authUrl consumed by WebView"
      pattern: "response_type=code|code_challenge"
    - from: "home_screen.dart"
      to: "anilist_service.dart"
      via: "_checkTokenFromUrl extracts code= instead of access_token="
      pattern: "code=|exchangeCode"
---

<objective>
Phase 1: PKCE Migration & Pairing Server Security Hardening

Purpose: Eliminate three CRITICAL/HIGH security vulnerabilities in the AniList OAuth integration — hardcoded client secret (SEC-01), unprotected pairing server binding (SEC-02), and unauthenticated /token endpoint (SEC-03) — by migrating from Implicit Grant to PKCE and adding CSRF, origin validation, and rate limiting to the pairing server.

Output: Secure end-to-end AniList pairing flow where the client secret is removed, the /token endpoint is protected, and token storage uses the shared LocalStorage abstraction.
</objective>

<execution_context>
@/home/jabs/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/jabs/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-security-hardening/01-CONTEXT.md
@.planning/codebase/CONCERNS.md
@.planning/STATE.md

<interfaces>
<!-- Key types and contracts from source files the executor needs -->

From lib/core/constants/app_constants.dart (current):
```dart
class AppConstants {
  static const String anilistClientId = '44217';
  static const String anilistClientSecret = '9JIiqT5UFM9YxG7RlaRiSatrOTjEqHJ3E0WaKpGD'; // REMOVE
  static const String anilistOAuth = 'https://anilist.co/api/v2/oauth/authorize';
  // ...other constants
}
```

From lib/core/anilist/anilist_service.dart (current exports/methods):
- `static String get authUrl` — builds OAuth authorize URL with `response_type=token` (needs PKCE update)
- `static Future<bool> isLoggedIn()` — checks SharedPreferences for token key
- `static Future<String?> getToken()` — reads from SharedPreferences directly
- `static Future<bool> saveToken(String token)` — saves to SharedPreferences directly, validates `startsWith('eyJ')`, fetches user
- `static Future<void> logout()` — removes token + user from SharedPreferences
- `static Future<AniListUser?> getUser()` — reads from SharedPreferences

From lib/core/storage/local_storage.dart (current pattern):
- `static Future<void> init()` — initializes SharedPreferences
- `static void ensureInitialized()` — throws if not initialized
- Methods use pattern: `_prefs?.setString('key', jsonEncode(data))`, `_prefs?.getString('key')`

From lib/core/cache/app_caches.dart (rate limit cache pattern):
- `static final TtlCache httpResponses = TtlCache(defaultTtl: Duration(minutes: 5), maxSize: 400)`

From lib/core/anilist/anilist_pairing_server.dart (current flow):
- `HttpServer.bind(InternetAddress.anyIPv4, port)` — binds to 0.0.0.0
- `_authorizeUrl` getter — builds Implicit Grant URL with `response_type=token`
- `_handleToken` — reads token from query param or POST body, calls `AniListService.saveToken`
- `_html` helper — sets `Access-Control-Allow-Origin: *`
- `_landingPage()` — HTML with link to `$_authorizeUrl`
- `_callbackPage()` — HTML+JS that reads `access_token` from URL fragment and POSTs to `/token`
- IP detection via `NetworkInterface.list()`
</interfaces>
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Write failing end-to-end tests for PKCE pairing flow</name>

  <files>test/security/pkce_pairing_test.dart</files>

  <read_first>
    - lib/core/anilist/anilist_pairing_server.dart (current Implicit Grant flow — understand structure to test against)
    - lib/core/anilist/anilist_service.dart (current authUrl and saveToken methods)
    - .planning/phases/01-security-hardening/01-CONTEXT.md (all decisions D-01 through D-08 for expected behavior)
  </read_first>

  <action>
    Create `test/security/pkce_pairing_test.dart` with these test groups. ALL tests must fail on first run because the server still uses Implicit Grant — this is the RED phase.

    Use `package:test/test.dart` (pure Dart tests) + `package:http/http.dart` for HTTP calls. Do NOT use `flutter_test` — these are integration-level tests against the HTTP server.

    Create these test groups:

    **PKCE Authorization Flow:**
    1. `GET / returns landing page with PKCE params` — verify body contains `code_challenge_method=S256`, `response_type=code`, `state=` param, `code_challenge=`
    2. `landing page authorize URL excludes client_secret` — verify redirect URL in the landing page's `href` does NOT contain `client_secret`
    3. `landing page redirect URL includes client_id` — verify `client_id=44217` is present

    **CSRF Protection:**
    4. `landing page has CSRF meta tag` — verify `<meta name="csrf-token" content="...">` exists with content length >= 32 chars
    5. `callback page JS includes csrf_token in POST body` — verify the callback HTML/JS references `csrf_token` in the POST to /token
    6. `POST /token without csrf_token returns 403` — send POST with `access_token=test`, verify status 403

    **Origin Validation:**
    7. `POST /token with wrong Origin returns 403` — send POST with `Origin: http://evil.com`, verify 403

    **Rate Limiting:**
    8. `6th POST /token from same IP within 60s returns 429` — send 5 rapid POSTs (all 200 or 403 depending on other checks), then 6th must return 429

    **CORS:**
    9. `Access-Control-Allow-Origin is restricted` — GET `/` and verify the response header is NOT `*` but matches the TV's own origin pattern

    **Remove client_secret usage:**
    10. `AniListService.exchangeCodeForToken exists` — import AniListService, verify `exchangeCodeForToken` is a callable static method with signature `Future<String?> exchangeCodeForToken(String code, String verifier, {String? redirectUri})`

    For each test, use `HttpServer.bind(InternetAddress.loopbackIPv4, 0)` to start the server in setUp, get the actual bound port from `server.port`, and dispose in tearDown.

    Structure:
    ```dart
    import 'dart:convert';
    import 'dart:io';
    import 'package:http/http.dart' as http;
    import 'package:test/test.dart';

    // Import paths TBD based on test directory location
    // For pure Dart tests outside lib/, relative imports may need package:goanime_tv/ prefix

    void main() {
      late AniListPairingServer server;
      late int port;

      setUp(() async {
        server = AniListPairingServer();
        final started = await server.start();
        if (!started) throw Exception('Server failed to start');
        port = server.port!; // Note: need to expose port or compute from pairUrl
      });

      tearDown(() => server.dispose());

      group('PKCE Authorization Flow', () {
        test('GET / returns landing page with PKCE params', () async {
          // ...
        });
        // ...
      });

      group('CSRF Protection', () {
        // ...
      });

      group('Origin Validation', () {
        // ...
      });

      group('Rate Limiting', () {
        // ...
      });

      group('CORS', () {
        // ...
      });
    }
    ```

    Since `AniListPairingServer.port` is currently a private `_port` field, the test will also need a way to get the port. Either test from `pairUrl` (which includes `$_ip:$_port`) or note that `port` getter needs to be exposed.

    The `AniListService.exchangeCodeForToken` test (test 10) checks that the method exists in the service, not that it works (that requires mocking HTTP). Use `AniListService.exchangeCodeForToken` — the test verifies the method signature.

    After writing, commit: `test(01-01): add failing PKCE pairing flow tests`
  </action>

  <behavior>
    - Test 1: Landing page contains PKCE params
    - Test 2: No client_secret in authorize URL
    - Test 3: client_id present in authorize URL
    - Test 4: CSRF meta tag present with >= 32 chars
    - Test 5: Callback JS contains csrf_token
    - Test 6: POST /token without csrf_token returns 403
    - Test 7: POST /token with wrong Origin returns 403
    - Test 8: 6th POST from same IP returns 429
    - Test 9: CORS header is restricted (not '*')
    - Test 10: AniListService.exchangeCodeForToken is callable
  </behavior>

  <verify>
    <automated>cd /home/jabs/codes-ai/goanime-tv && dart test test/security/pkce_pairing_test.dart 2>&1 | head -30; echo "EXIT: $?"</automated>
    <human-check>The test runner output shows FAIL for each test. At least 8/10 tests should fail because they test features that do not exist yet.</human-check>
  </verify>

  <done>
    - `test/security/pkce_pairing_test.dart` exists with 10 tests across 5 groups (PKCE, CSRF, Origin, RateLimiting, CORS)
    - Each test document expects specific behavior from the PKCE-enabled server
    - Running the tests produces failures for at least 8 of the 10 tests (confirming the RED phase)
  </done>

  <acceptance_criteria>
    - File `test/security/pkce_pairing_test.dart` exists at minimum 80 lines
    - File contains `import 'package:test/test.dart'`
    - File contains `import 'package:http/http.dart' as http`
    - File contains `code_challenge_method=S256` as an expected string
    - File contains `csrf_token` string references
    - File contains test group names: 'PKCE Authorization Flow', 'CSRF Protection', 'Origin Validation', 'Rate Limiting', 'CORS'
    - Running `dart test test/security/pkce_pairing_test.dart` produces failures (RED phase confirmed)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Implement PKCE pairing flow (thinnest vertical slice)</name>

  <files>
    lib/core/anilist/anilist_pairing_server.dart
    lib/core/constants/app_constants.dart
    lib/core/anilist/anilist_service.dart
  </files>

  <read_first>
    - test/security/pkce_pairing_test.dart (test expectations — the contract)
    - lib/core/anilist/anilist_pairing_server.dart (current state — rewrite target)
    - lib/core/constants/app_constants.dart (current state — remove clientSecret)
    - lib/core/anilist/anilist_service.dart (current state — add PKCE exchange)
    - .planning/phases/01-security-hardening/01-CONTEXT.md (decisions D-01, D-02)
  </read_first>

  <action>
    Implement the PKCE authorization code flow end-to-end. This is the thinnest slice that makes the PKCE-related tests pass. CSRF, origin validation, rate limiting, CORS restriction, and LocalStorage migration are NOT needed here — they come in Task 3.

    **1. Remove hardcoded client_secret from `lib/core/constants/app_constants.dart`:**
    - Delete line 13 (`static const String anilistClientSecret = '...'`)

    **2. Add PKCE exchange method to `lib/core/anilist/anilist_service.dart`:**
    - Add `static Future<String?> _codeVerifier` field (stores PKCE verifier for WebView flow)
    - Add `static String _generateCodeVerifier()` — returns 128-byte base64url random string via `Random.secure()`
    - Add `static String _sha256Base64Url(String input)` — computes SHA-256 of input, returns base64url-encoded digest without padding. Use `crypto` package (add to pubspec.yaml dependencies if needed) OR implement via `dart:convert` + `dart:typed_data` manual implementation. Prefer adding `crypto: ^3.0.0` to pubspec.yaml under dependencies (check pub.dev for latest version). Note: `convert` package already imported in `anilist_service.dart`.
      - Actually, the project already has `encrypt: ^5.0.0` and `pointycastle: ^3.7.3`. Use `package:pointycastle` for SHA-256 to avoid adding a new dependency.
    - Add `static String get authUrl` — change to PKCE: generates code_verifier, computes S256 code_challenge, generates random state. Returns URL with: `response_type=code`, `client_id=44217`, `code_challenge=<s256-base64url>`, `code_challenge_method=S256`, `state=<random>`.
      - The redirect_uri can be omitted (AniList uses its default) since we intercept via WebView NavigationDelegate.
    - Add `static Future<String?> exchangeCodeForToken(String code, String verifier, {String? redirectUri})` — POST to `https://anilist.co/api/v2/oauth/token` with form body: `grant_type=authorization_code`, `client_id=44217`, `code=<code>`, `code_verifier=<verifier>`, `redirect_uri=<redirectUri>` (default: `'https://anilist.co/api/v2/oauth/callback'`). Parse JSON response, extract `access_token`, call `saveToken(token)`, return the token string on success, null on failure.

    **3. Rewrite `lib/core/anilist/anilist_pairing_server.dart` for PKCE:**
    - Keep the class structure and constructor. Keep `_server`, `_loggedIn`, `_ip`, `_port` fields.
    - Add `String? _codeVerifier` (stored per-session between landing page and callback)
    - Add `String? _state` (stored per-session)
    - Add `String? _csrfToken` (placeholder — actual CSRF validation in Task 3)
    - Keep `start()`, `dispose()`, `_lanIp()`, `pairUrl`, `isRunning`, `onLoggedIn` as-is.
    - Keep `HttpServer.bind(InternetAddress.anyIPv4, port)` (per D-03 — keep LAN access).
    - Replace `_authorizeUrl`:
      - Generate `_codeVerifier = _randomBase64Url(128)` (via `Random.secure()`)
      - Compute `_codeChallenge = _sha256Base64Url(_codeVerifier!)`
      - Generate `_state = _randomBase64Url(32)`
      - Return URL: `https://anilist.co/api/v2/oauth/authorize?client_id=${AppConstants.anilistClientId}&response_type=code&code_challenge=${_codeChallenge}&code_challenge_method=S256&state=${_state}&redirect_uri=${Uri.encodeComponent(callbackUrl)}`
      - The `callbackUrl` is `http://$_ip:$_port/callback`
    - Update `_landingPage()`:
      - Keep the styling and card structure
      - Change link href to use the new `$_authorizeUrl` (PKCE version)
      - Add `<meta name="csrf-token" content="">` to head (empty for now — Task 3 adds real value)
    - Update `_callbackPage()`:
      - Rewrite JS to read `code` from query params (`?code=...&state=...`) instead of `access_token` from fragment
      - Include placeholders: `var csrfToken = document.querySelector('meta[name="csrf-token"]').getAttribute('content');` (reads the meta tag that the landing page set)
      - POST body: `'code=' + encodeURIComponent(code) + '&state=' + encodeURIComponent(state) + '&csrf_token=' + encodeURIComponent(csrfToken)` — NOTE: the server doesn't validate CSRF yet (Task 3)
      - JS should parse query params: `function q(name){ var m = location.search.match(new RegExp('[?&]' + name + '=([^&]*)')); return m ? decodeURIComponent(m[1]) : ''; }`
      - After successful POST display: "Pronto! ✅" on success, "Falhou" on failure
    - Update `_handleToken` to:
      - Accept POST body (application/x-www-form-urlencoded or plain text)
      - Parse `code`, `state`, `csrf_token` from body
      - Validate `state` matches stored `_state`
      - Call `AniListService.exchangeCodeForToken(code, _codeVerifier!)` to exchange via AniList
      - Save token if successful, complete `_loggedIn`
      - For now, do NOT validate CSRF token — that comes in Task 3
      - Return `_resultPage(ok)` with appropriate status code
    - Keep `_handle()` routing logic but update `_handleToken` call
    - Keep `_resultPage()` as-is (relatively stable)
    - Remove `Access-Control-Allow-Origin: *` from `_html` helper — replace with TV's own origin from `$_ip:$_port` (exact CORS restriction comes in Task 3, but the wildcard must go now per D-08)
    - ALL `Random.secure()` calls for code_verifier and CSRF token generation should use `dart:math` `Random.secure()` (recommended by discretion area in CONTEXT.md)

    Helper methods to add to pairing server:
    - `String _randomBase64Url(int byteLength)` — generates cryptographically random bytes, base64url-encodes, strips padding
    - `String _sha256Base64Url(String input)` — SHA-256 hash, base64url-encode, no padding (use PointyCastle or dart:convert base64)

    After implementing, verify tests from Task 1 pass:
    ```bash
    cd /home/jabs/codes-ai/goanime-tv && dart test test/security/pkce_pairing_test.dart
    ```

    At minimum test 1, 2, 3 (PKCE flow tests) should pass. Tests 4-9 (CSRF, origin, rate limiting, CORS) may still fail — that's expected for this task.

    Commit: `feat(01-01): migrate AniList OAuth from Implicit Grant to PKCE`
  </action>

  <verify>
    <automated>cd /home/jabs/codes-ai/goanime-tv && dart test test/security/pkce_pairing_test.dart 2>&1</automated>
  </verify>

  <done>
    - `app_constants.dart` line 13 (`anilistClientSecret`) is removed
    - `AniListService.authUrl` returns PKCE authorization URL with `code_challenge_method=S256`
    - `AniListService.exchangeCodeForToken()` exists and calls AniList's `/oauth/token` endpoint
    - `anilist_pairing_server.dart` landing page includes PKCE params in the authorize URL
    - `anilist_pairing_server.dart` callback page JS reads `code` from query string instead of `access_token` from fragment
    - `anilist_pairing_server.dart` `_handleToken` validates `state` parameter and calls `exchangeCodeForToken`
    - Access-Control-Allow-Origin is no longer `*`
    - PKCE-related tests (1, 2, 3) pass in the test suite
  </done>

  <acceptance_criteria>
    - `app_constants.dart` does NOT contain `anilistClientSecret` — grep for `anilistClientSecret` returns 0 matches in this file
    - `app_constants.dart` still contains `anilistClientId = '44217'`
    - `anilist_service.dart` contains `exchangeCodeForToken(` with parameters `code`, `verifier`, `redirectUri`
    - `anilist_service.dart` `authUrl` getter contains `response_type=code`
    - `anilist_service.dart` `authUrl` getter contains `code_challenge_method=S256`
    - `anilist_service.dart` `authUrl` getter does NOT contain `response_type=token`
    - `anilist_pairing_server.dart` landing page HTML contains `code_challenge_method=S256`
    - `anilist_pairing_server.dart` landing page HTML contains `response_type=code`
    - `anilist_pairing_server.dart` callback page JS reads `code` from query string (not `access_token` from fragment)
    - `anilist_pairing_server.dart` `_html` helper does NOT set `Access-Control-Allow-Origin: *`
    - `dart test test/security/pkce_pairing_test.dart` — tests 1, 2, 3 pass (tests 4-9 may still fail)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Harden /token endpoint + migrate token storage to LocalStorage</name>

  <files>
    lib/core/anilist/anilist_pairing_server.dart
    lib/core/storage/local_storage.dart
    lib/core/anilist/anilist_service.dart
  </files>

  <read_first>
    - lib/core/anilist/anilist_pairing_server.dart (post-Task-2 state — add protections)
    - lib/core/storage/local_storage.dart (current state — add token helpers)
    - lib/core/anilist/anilist_service.dart (post-Task-2 state — migrate storage calls)
    - lib/core/cache/app_caches.dart (TTL cache pattern for rate limiting — use `httpResponses` TTL pattern)
    - .planning/phases/01-security-hardening/01-CONTEXT.md (decisions D-03, D-04, D-05, D-06, D-07, D-08, D-09)
    - test/security/pkce_pairing_test.dart (remaining failing tests — the contract)
  </read_first>

  <action>
    Add security protections to the /token endpoint and migrate token storage. This makes the remaining tests pass.

    **1. Add CSRF token validation to pairing server (`anilist_pairing_server.dart`):**
    - In `_landingPage()`: replace placeholder `content=""` with real token: `content="${_csrfToken}"` where `_csrfToken = _randomBase64Url(32)` (generated during landing page construction, stored per-session)
    - In `_handleToken()`: extract `csrf_token` from POST body. If missing or doesn't match `_csrfToken`, return 403. Invalidate token after first successful use (single-use semantics per D-05 sketch).
    - The `_csrfToken` should be generated fresh for each landing page load (when GET `/` is called). Store it in `_csrfToken` field and null it out after validation.

    **2. Add Origin/Referer header validation (`anilist_pairing_server.dart`):**
    - In `_handleToken()`: extract `Origin` and `Referer` headers from the request
    - If `Origin` is present, verify it starts with `http://$_ip` (TV's own origin)
    - If `Referer` is present, verify it starts with `http://$_ip` (TV's own origin)
    - If at least one is present and fails validation, return 403
    - Per D-06

    **3. Add rate limiting (`anilist_pairing_server.dart`):**
    - Add `final _rateLimitStore = <String, int>{};` and `_rateLimitReset = <String, int>{};` (ip → count, ip → window start)
    - In `_handleToken()`: get client IP from `req.connectionInfo?.remoteAddress.address`. Check if count >= 5 within the current 60s window. If exceeded, return 429. Otherwise increment.
    - Clean up stale entries periodically or lazily (check if window expired, reset counter)
    - TTL-based approach: store `{count, windowStart}` per IP. Reset if `now - windowStart > 60000`.
    - Per D-07: max 5 POST/60s per client IP

    **4. Set CORS restriction (`anilist_pairing_server.dart`):**
    - In `_html()` helper: set `Access-Control-Allow-Origin` to `http://$_ip:$_port` (TV's own origin)
    - Remove any fallback or wildcard CORS headers
    - Per D-08

    **5. Add LAN security documentation to pairing page (`anilist_pairing_server.dart`):**
    - In `_landingPage()` HTML: add a small warning paragraph below the card: "⚠ Este servidor é apenas para uso em rede local. O token de acesso é transmitido por HTTP em sua rede local."
    - Per D-04

    **6. Migrate AniListService token storage to LocalStorage:**
    - Add token helper methods to `lib/core/storage/local_storage.dart`:
      - `static Future<bool> saveToken(String token)` — stores `token` under key `'anilist_token'`, validates starts with `'eyJ'`, returns true on success
      - `static String? getToken()` — reads from key `'anilist_token'`, returns null if missing
      - `static Future<void> removeToken()` — removes key `'anilist_token'`
      - `static Future<bool> saveUserData(String key, Map<String, dynamic> data)` — stores user data under `'anilist_user'` (naming follows existing patterns: `_prefs?.setString('anilist_$key', jsonEncode(data))`)
      - `static Map<String, dynamic>? getUserData(String key)` — reads `'anilist_user'`, decodes JSON
      - Follow existing patterns: `ensureInitialized()` before access, `_prefs?.setString()`, `_prefs?.getString()`, `_prefs?.remove()`
    - Update `lib/core/anilist/anilist_service.dart`:
      - Replace all `SharedPreferences.getInstance()` calls with `LocalStorage.getToken()` / `LocalStorage.saveToken()` / `LocalStorage.removeToken()`
      - Specifically: `isLoggedIn()` → use `LocalStorage.getToken() != null`
      - `getToken()` → delegate to `LocalStorage.getToken()`
      - `saveToken(String token)` → delegate validation + saving to LocalStorage; keep the `_fetchUser` logic but save user data via `LocalStorage.saveUserData()`
      - `logout()` → call `LocalStorage.removeToken()`
      - `getUser()` → use `LocalStorage.getUserData()`
      - Remove `SharedPreferences` import from `anilist_service.dart` (no longer used directly)
    - Per D-09

    After all changes, run the full test suite:
    ```bash
    cd /home/jabs/codes-ai/goanime-tv && dart test test/security/pkce_pairing_test.dart
    ```
    All 10 tests should pass.

    Commit: `feat(01-01): harden /token endpoint with CSRF, origin validation, rate limiting; migrate token storage to LocalStorage`
  </action>

  <verify>
    <automated>cd /home/jabs/codes-ai/goanime-tv && dart test test/security/pkce_pairing_test.dart 2>&1</automated>
  </verify>

  <done>
    - CSRF token validation on /token: POST without valid token returns 403
    - Origin/Referer header validation: POST from wrong origin returns 403
    - Rate limiting: 6th POST within 60s from same IP returns 429
    - CORS restricted to TV's own origin (not `*`)
    - Landing page shows LAN-security warning in Portuguese
    - LocalStorage has saveToken/getToken/removeToken/saveUserData/getUserData methods
    - AniListService delegates all storage to LocalStorage (no direct SharedPreferences calls)
    - All 10 tests pass in `pkce_pairing_test.dart`
  </done>

  <acceptance_criteria>
    - `anilist_pairing_server.dart` contains CSRF token generation using `Random.secure()`
    - `anilist_pairing_server.dart` `_handleToken` checks `csrf_token` from POST body against stored `_csrfToken`
    - `anilist_pairing_server.dart` `_handleToken` validates `Origin`/`Referer` headers match `http://$_ip`
    - `anilist_pairing_server.dart` `_handleToken` increments rate limit counter and rejects with 429 at 6th request within 60s
    - `anilist_pairing_server.dart` `_html` sets `Access-Control-Allow-Origin` to `http://$_ip:$_port` (not `*`)
    - `anilist_pairing_server.dart` `_landingPage()` contains "Este servidor é apenas para uso em rede local"
    - `local_storage.dart` contains `static Future<bool> saveToken(String token)` and `static String? getToken()`
    - `anilist_service.dart` does NOT import `shared_preferences` — grep for `import.*shared_preferences` returns 0
    - `anilist_service.dart` `saveToken` calls `LocalStorage.saveToken()` not `SharedPreferences`
    - All 10 tests in `pkce_pairing_test.dart` pass — `dart test test/security/pkce_pairing_test.dart` exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phone browser → TV pairing server | Untrusted client on LAN sends HTTP requests to TV-bound server |
| TV pairing server → AniList API | Server exchanges auth code for token via HTTPS to AniList |
| WebView (TV app) → AniList OAuth | Embedded browser navigates to AniList, intercepts redirect with auth code |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Spoofing | /token POST body | mitigate | PKCE code exchange requires valid `code_verifier` — attacker cannot forge valid auth code |
| T-01-02 | Tampering | /token POST body | mitigate | CSRF token (32-byte random, single-use) must match between landing page and POST; missing/wrong token returns 403 |
| T-01-03 | Information Disclosure | /token endpoint | mitigate | Origin/Referer header validation rejects cross-origin requests; CORS restricted to TV's own origin |
| T-01-04 | Denial of Service | /token endpoint | mitigate | Rate limiting: max 5 POST/60s per client IP, returns 429 |
| T-01-05 | Information Disclosure | HTTP LAN traffic | accept | D-03: TLS impractical on LAN (mobile browser rejects self-signed cert). D-04: documented warning on pairing page |
| T-01-06 | Information Disclosure | Hardcoded secret | mitigate | D-01/D-02: PKCE eliminates need for client_secret entirely. Secret removed from app_constants.dart |
| T-01-SC | Tampering | pubspec.yaml (crypto dependency) | mitigate | Add `crypto: ^3.0.0` or use existing `pointycastle` for SHA-256 — verify on pub.dev before install. No [SUS] packages detected. |
</threat_model>

<verification>
1. Run test suite: `cd /home/jabs/codes-ai/goanime-tv && dart test test/security/pkce_pairing_test.dart` — all 10 tests must pass
2. Verify client_secret removed: `grep -c anilistClientSecret lib/core/constants/app_constants.dart` returns 0
3. Verify no Access-Control-Allow-Origin: `grep -c 'Access-Control-Allow-Origin.*\*' lib/core/anilist/anilist_pairing_server.dart` returns 0
4. Verify SharedPreferences not used directly in AniListService: `grep -c 'shared_preferences' lib/core/anilist/anilist_service.dart` returns 0
5. Visual verification: build and run the app, trigger pairing, confirm the flow works end-to-end (manual checkpoint after all tasks)
</verification>

<success_criteria>
- [x] SEC-01: Hardcoded client_secret removed from `app_constants.dart` — PKCE migration eliminates need (D-01)
- [x] SEC-02: LAN access preserved (0.0.0.0) with documented warning (D-03, D-04); CSRF protection on /token (D-05)
- [x] SEC-03: CSRF token flow (D-05), Origin/Referer validation (D-06), rate limiting (D-07) implemented on /token
- [x] CORS wildcard removed, restricted to TV origin (D-08)
- [x] Token storage migrated to LocalStorage (D-09)
- [x] All existing pairing functionality preserved — PKCE flow works end-to-end
- [x] All 10 integration tests pass
</success_criteria>

<output>
Create `.planning/phases/01-security-hardening/01-01-SUMMARY.md` when done
</output>
