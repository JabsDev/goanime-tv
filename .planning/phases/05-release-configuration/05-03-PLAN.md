---
phase: 05-release-configuration
plan: 03
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - docs/RELEASE_CHECKLIST.md
  - assets/store/screenshot_specs.md
  - assets/privacy_policy.md
  - AGENTS.md
autonomous: true
requirements: [REL-03]
user_setup: []

must_haves:
  truths:
    - "Release checklist documents every step from version bump through Play Store submission"
    - "Play Store listing asset directory exists with placeholder spec documentation"
    - "Privacy policy covers data collection by AniList OAuth and scrape sources"
    - "AGENTS.md references CI workflow and release process"
    - "Developer can follow the checklist end-to-end to submit v1.0 to Play Store"
  artifacts:
    - path: "docs/RELEASE_CHECKLIST.md"
      provides: "Play Store submission step-by-step guide"
      min_lines: 60
    - path: "assets/store/screenshot_specs.md"
      provides: "Screenshot requirements documentation"
      contains: "1920x1080"
    - path: "assets/privacy_policy.md"
      provides: "Privacy policy source document"
      contains: "AniList"
    - path: "AGENTS.md"
      provides: "Updated developer guidance"
      contains: "CI"
  key_links:
    - from: "docs/RELEASE_CHECKLIST.md"
      to: "Play Console"
      via: "Step 4: Upload to Play Console"
    - from: "assets/privacy_policy.md"
      to: "AniList OAuth"
      via: "AniList API usage and data collection disclosure"

---

## Phase Goal

Continuing from Plans 01-02 — same derived user story:

**As a** developer, **I want to** configure Android app signing, CI/CD automation, and Play Store listing assets, **so that** I can submit v1.0 of GoAnime TV to the Play Store.

## Multi-Source Coverage Audit

### GOAL: "Configure build pipeline and release assets for Play Store submission"

All items from multi-source audit covered in Plan 01's audit table. This plan covers the release checklist + Play Store listing assets sub-items.

### CONTEXT: Decisions Implemented

| Decision | How this plan implements |
|----------|-------------------------|
| D-05 (the agent's discretion) | Play Store listing: screenshots 1920x1080, feature graphic 1024x500, description in PT-BR with English secondary. Documented in screenshot_specs.md and release checklist. |
| D-05 (the agent's discretion) | Privacy policy covers AniList OAuth data + scraped source data. Documented in privacy_policy.md. |
| REL-03 | Checklist covers version bump, tag push, CI verification, CI artifact download, Play Console upload, store listing completion, privacy policy link, internal testing track submission. |

<objective>
**What:** Create the release checklist document, Play Store listing asset directory, privacy policy source document, and update AGENTS.md.

**Purpose:** Provide the developer with a complete, step-by-step Play Store submission guide and all required listing documentation. After this plan, the developer has everything needed (except binary screenshots) for a Play Store submission.

**Output:**
- `docs/RELEASE_CHECKLIST.md` — step-by-step Play Store submission checklist
- `assets/store/` directory with `screenshot_specs.md` — screenshot requirement specs
- `assets/privacy_policy.md` — privacy policy covering AniList and scraper data
- `AGENTS.md` — updated with CI workflow and release process references
</objective>

<execution_context>
@/home/jabs/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/jabs/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-release-configuration/05-CONTEXT.md
@.planning/phases/05-release-configuration/05-RESEARCH.md
@.planning/phases/05-release-configuration/05-PATTERNS.md
@AGENTS.md

<interfaces>
<!-- Existing document formats to follow -->

From AGENTS.md (section headings style):
- `## Build & Run` — bash code blocks for commands
- `## Project Structure` — tree-style ASCII directory
- `## Testing` — commands with descriptions
- `## Workflow` — bullet list of GSD commands
- `## Architecture Notes` — bullet list of key architectural decisions

From README.md (Markdown conventions):
- `#` for title, `##` for sections, `###` for subsections
- `- ` bullet lists for items
- ```bash for code blocks
- `---` horizontal rules for separation

Play Store asset specs (from RESEARCH.md lines 196-204):
- Screenshots: 1920x1080 landscape (16:9), Android TV, 1-8 screenshots
- TV banner: 1280x720
- Feature graphic: 1024x500
- Description: PT-BR primary, English secondary
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create release checklist document with full Play Store submission steps</name>
  <files>docs/RELEASE_CHECKLIST.md</files>
  <read_first>AGENTS.md</read_first>
  <action>
    Create the `docs/` directory (does not exist yet) and write `docs/RELEASE_CHECKLIST.md`.

    The release checklist covers the complete end-to-end release flow:

    **Title:** `# GoAnime TV — Release Checklist v1.0`

    **Sections (each `## `):**

    1. **Prerequisites** — Checklist items with `- [ ] ` format:
       - [ ] Play Console account created ($25 one-time fee) and Developer Registration completed
       - [ ] Google Play App Signing enrolled (checked during first upload)
       - [ ] Upload keystore generated (`keytool -genkey -v -keystore android/app/upload-keystore.jks -alias goanime-tv-upload -keyalg RSA -keysize 2048 -validity 10000`)
       - [ ] Keystore backup saved to secure location (NOT in repo)
       - [ ] GitHub Secrets configured: ANDROID_KEYSTORE_BASE64, ANDROID_KEYSTORE_PASSWORD, ANDROID_KEY_ALIAS, ANDROID_KEY_PASSWORD
       - [ ] All Play Store listing assets prepared (screenshots, banner, feature graphic, privacy policy)

    2. **Version Bump**
       - [ ] Update `pubspec.yaml` version (e.g., `1.0.0+1000000` for v1.0.0). Per D-06: versionCode = major*1000000 + minor*10000 + patch.
       - [ ] Run `flutter pub get` to verify version resolves
       - [ ] Commit: `git commit -m "chore: bump version to X.Y.Z"`
       - [ ] Create and push tag: `git tag vX.Y.Z && git push origin vX.Y.Z`

    3. **CI Verification**
       - [ ] Wait for GitHub Actions release workflow to complete (check Actions tab)
       - [ ] Verify test job passed (flutter analyze + flutter test)
       - [ ] Verify release job completed successfully (Go FFI build + signed APK + AAB)
       - [ ] Download signed APK + AAB from workflow artifacts (`goanime-tv-release-vX.Y.Z`)

    4. **Local Verification**
       - [ ] Install APK on Android TV device or emulator: `flutter install build/app/outputs/flutter-apk/app-release.apk`
       - [ ] Verify search, detail, and playback flows work correctly
       - [ ] Verify no debug print statements visible in release build (check `flutter logs`)
       - [ ] Verify app icon and name display correctly

    5. **Play Store Listing**
       - [ ] Create or update store listing in Play Console
       - [ ] Full description (PT-BR): `Aplicativo Android TV para assistir anime de múltiplas fontes...`
       - [ ] Short description (PT-BR): `Assista anime de AnimeFire, AllAnime, SuperFlix e Goyabu em um só lugar`
       - [ ] Full description (English): `Android TV app for watching anime from multiple sources...`
       - [ ] Short description (English): `Watch anime from AnimeFire, AllAnime, SuperFlix and Goyabu in one place`
       - [ ] Upload screenshots (1920×1080 landscape, 16:9) — at least 2, up to 8
       - [ ] Upload TV banner (1280×720)
       - [ ] Upload feature graphic (1024×500)
       - [ ] Set app category: Entertainment
       - [ ] Set content rating: Complete questionnaire

    6. **Privacy & Data Safety**
       - [ ] Host privacy_policy.md (from assets/) at a public URL (GitHub Pages,或个人网站)
       - [ ] Enter privacy policy URL in Play Console
       - [ ] Complete Data Safety section in Play Console:
         - Data collected: Personal info (optional, AniList OAuth name/avatar)
         - Data collected: App activity (watch history, favorites — stored locally)
         - No data shared with third parties (scraped sources are read-only)
       - [ ] Declare ads: No

    7. **App Content**
       - [ ] Target SDK: Comply with 2026 requirements (targetSdk 35)
       - [ ] 64-bit architecture: Verified (arm64-v8a + x86_64 .so files exist)
       - [ ] 16KB page size: Verify Go shared library alignment (research needed — see RESEARCH.md Open Questions)
       - [ ] Android TV requirements: Leanback launcher icon, TV-optimized, gamepad support not needed
       - [ ] Complete "App content" section in Play Console

    8. **Production Deployment**
       - [ ] Upload AAB (`build/app/outputs/bundle/release/app-release.aab`) to Play Console
       - [ ] Review release details (release name, release notes/what's new)
       - [ ] Select rollout percentage (start at 100% for initial release or staged rollout)
       - [ ] Submit for review
       - [ ] Monitor review status in Play Console (typically 1-3 days)
       - [ ] After approval: App goes live on Play Store

    9. **Post-Release**
       - [ ] Verify app appears in Play Store search
       - [ ] Install from Play Store on Android TV device
       - [ ] Verify all features work in production build
       - [ ] Monitor for crash reports (Play Console ANR & crash reporting)

    **Release Notes (Whats New) template:**
    ```
    PT-BR:
    • Primeiro lançamento do GoAnime TV
    • Assista anime de AnimeFire, AllAnime, SuperFlix e Goyabu
    • Pesquisa integrada com resultados de múltiplas fontes
    • Player de vídeo com seleção de qualidade
    • Integração com AniList para listas e descoberta
    
    English:
    • First release of GoAnime TV
    • Watch anime from AnimeFire, AllAnime, SuperFlix, and Goyabu
    • Integrated search with multi-source results
    • Video player with quality selection
    • AniList integration for lists and discovery
    ```

    Add a **Version History** section at the bottom tracking `1.0.0` → initial release.

    Use Markdown checklist syntax (`- [ ] `) throughout. Each major section starts with `## `.
  </action>
  <verify>
    <automated>wc -l docs/RELEASE_CHECKLIST.md && grep -c "## " docs/RELEASE_CHECKLIST.md && grep -c "\[ \]" docs/RELEASE_CHECKLIST.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/RELEASE_CHECKLIST.md` exists and is at least 60 lines
    - Document has at least 5 `## ` sections
    - Document contains checklist items with `- [ ] ` syntax
    - Document covers: version bump, CI verification, local verification, Play Store listing, privacy/data safety, production deployment
    - Document includes versionCode formula reference: `major*1000000 + minor*10000 + patch`
    - Document references `pubspec.yaml` for version source of truth
    - Document references the CI workflow (Plan 02) for release builds
    - Document includes 16KB page size verification step
  </acceptance_criteria>
  <done>docs/RELEASE_CHECKLIST.md exists with 9 sections covering prerequisites through post-release; checklist items use - [ ] format; all Play Store steps documented</done>
</task>

<task type="auto">
  <name>Task 2: Create Play Store asset directory with screenshot spec doc</name>
  <files>assets/store/screenshot_specs.md</files>
  <read_first>AGENTS.md</read_first>
  <action>
    Create the `assets/store/` directory and write `assets/store/screenshot_specs.md`.

    This file documents the Play Store listing asset requirements for the developer creating screenshots. It is NOT a binary image — it's a reference document.

    Contents:

    **Title:** `# Play Store Listing Assets — GoAnime TV v1.0`

    **Section: Screenshots**
    - Format: PNG or JPEG
    - Dimensions: 1920×1080 pixels (landscape, 16:9) — required for Android TV
    - Count: 2–8 screenshots
    - Content guidance:
      - Screenshot 1: Home screen with catalog rows (trending, seasonal, continue watching)
      - Screenshot 2: Search screen with keyboard overlay
      - Screenshot 3: Anime detail screen with episode grid
      - Screenshot 4: Video player with playback controls
      - Screenshot 5: Profile screen (history, favorites, AniList login)
      - Additional: Show multi-source selector, quality picker, AniList enrichment
    - Naming convention: `screenshot_01.png`, `screenshot_02.png`, ... (saved in `assets/store/`)
    - Warning: Do not include copyrighted artwork as primary screenshot content. Use app UI screens showing the app in use. Avoid any content that could trigger copyright takedown.

    **Section: TV Banner**
    - Format: PNG or JPEG
    - Dimensions: 1280×720 pixels
    - Content: App logo + tagline on dark background
    - File: `tv_banner.png` (saved in `assets/store/`)

    **Section: Feature Graphic**
    - Format: PNG or JPEG
    - Dimensions: 1024×500 pixels
    - Content: App branding image for Play Store listing
    - File: `feature_graphic.png` (saved in `assets/store/`)

    **Section: App Icon**
    - Format: PNG
    - Dimensions: 512×512 pixels (Play Store), 192×192 pixels (Android TV)
    - Already exists in `android/app/src/main/res/mipmap-*/` — verify they are TV-optimized (no unnecessary white backgrounds)

    Add a note: "Create these images using your preferred tool (Figma, Photoshop, GIMP, or automated screenshot capture from emulator). These are binary assets that cannot be generated by code alone."
  </action>
  <verify>
    <automated>ls assets/store/screenshot_specs.md && grep -c "1920x1080" assets/store/screenshot_specs.md</automated>
  </verify>
  <acceptance_criteria>
    - `assets/store/screenshot_specs.md` exists
    - Document specifies 1920×1080 landscape screenshots (1-8)
    - Document specifies 1280×720 TV banner
    - Document specifies 1024×500 feature graphic
    - Document includes naming convention: `screenshot_01.png`, `tv_banner.png`, `feature_graphic.png`
    - Document includes content guidance for each screenshot type
  </acceptance_criteria>
  <done>assets/store/screenshot_specs.md documents all Play Store asset requirements with dimensions, naming, and content guidance</done>
</task>

<task type="auto">
  <name>Task 3: Create privacy policy and update AGENTS.md with CI/release references</name>
  <files>assets/privacy_policy.md, AGENTS.md</files>
  <read_first>AGENTS.md</read_first>
  <action>
    Two files to create/modify:

    **File 1: `assets/privacy_policy.md`**

    Create the privacy policy source document. This is a comprehensive privacy policy covering:
    - App name: GoAnime TV
    - Data controller: The developer (individual, named)
    - Data collected:
      - AniList OAuth: name, avatar URL, AniList user ID (optional, only if user logs in)
      - Watch history: episode titles, progress timestamps, favorites (stored locally on device via SharedPreferences)
      - No personal information is collected by the app server-side
      - No analytics SDKs, no crash reporting SDKs, no advertising SDKs
    - Data sources:
      - AnimeFire (animefire.net) — publicly available anime metadata
      - AllAnime (allanime.to) — publicly available anime metadata
      - SuperFlix (superflix.pro) — publicly available anime metadata
      - Goyabu (goyabu.com) — publicly available anime metadata
      - These sources are scraped client-side. No data is sent to third parties.
    - Data sharing: No user data is shared with any third party
    - Data retention: Watch history and preferences stored locally. AniList token stored locally. User can clear data via app settings.
    - Third-party services:
      - AniList API (GraphQL) — used for catalog enrichment and user lists. Subject to AniList's own privacy policy.
      - GitHub (source code repository) — no user data transmitted
    - Children's privacy: App does not knowingly collect data from children under 13
    - Changes: Policy may be updated. Users will be notified via app update.
    - Contact: Developer email placeholder (update before publishing)
    - Effective date: July 2026

    Format: Markdown with `## ` section headings. Place `[UPDATE: your-email@example.com]` and `[UPDATE: developer-name]` placeholders for the developer to fill.

    **File 2: `AGENTS.md` — update with CI and release process**

    Make two additions to AGENTS.md:

    1. **Add a CI section** after the "Build & Run" section (after line 17):
    ```
    ## CI/CD

    The project uses GitHub Actions for CI:

    - **Test pipeline:** Triggered on every push/PR to `main`. Runs `flutter analyze` + `flutter test`.
    - **Release pipeline:** Triggered by pushing a version tag (`v1.0.0`). Builds Go FFI library, then produces signed APK + AAB artifacts.
    - **Artifacts:** Signed APK and AAB are available for download from the Actions tab (90-day retention).

    To trigger a release:
    ```bash
    # After bumping version in pubspec.yaml
    git tag v1.0.0
    git push origin v1.0.0
    # Wait for CI to complete, then download artifacts from GitHub Actions
    ```
    ```

    2. **Update the "Workflow" section** to mention the release checklist:
    ```
    - `/gsd-progress` — View project progress
    - [RELEASE_CHECKLIST.md](docs/RELEASE_CHECKLIST.md) — Play Store submission steps
    ```

    Make sure to use triple backtick fenced code blocks with appropriate language identifiers for bash. Preserve all existing content.
  </action>
  <verify>
    <automated>grep -c "AniList" assets/privacy_policy.md && grep -c "CI" AGENTS.md && grep -c "RELEASE_CHECKLIST" AGENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `assets/privacy_policy.md` exists and covers: data collected (AniList OAuth, watch history), data sources (all 4 scrapers + AniList), data sharing (none), third-party services, children's privacy, contact placeholder
    - `assets/privacy_policy.md` contains `AniList`, `AnimeFire`, `AllAnime`, `SuperFlix`, `Goyabu` service references
    - `assets/privacy_policy.md` contains `[UPDATE:` placeholders for email and developer name
    - `AGENTS.md` contains `## CI/CD` section with test/release pipeline description
    - `AGENTS.md` contains `RELEASE_CHECKLIST.md` reference in Workflow section
    - `AGENTS.md` still contains all original content (Build & Run, Project Structure, Testing, Architecture Notes)
  </acceptance_criteria>
  <done>assets/privacy_policy.md covers all data collection/disclosure requirements; AGENTS.md updated with CI/CD section and release checklist reference</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Privacy policy → Play Store | Privacy policy must accurately reflect data collection. Inaccurate or incomplete policy can result in app suspension. |
| Checklist → developer actions | Checklist guides developer through Play Store submission. Errors in documentation can lead to submission rejection. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-12 | Information Disclosure | Privacy policy omits data collection | mitigate | Policy covers all data flows: AniList OAuth (name, avatar), local storage (watch history, favorites). Scraped sources (AnimeFire, AllAnime, SuperFlix, Goyabu) are read-only client-side fetches — no user data sent to them. |
| T-05-13 | Spoofing | Checklist directs to phishing Play Console URL | mitigate | All URLs in checklist are official Google domains (play.google.com/console, developer.android.com). No shortened URLs. |
| T-05-14 | Information Disclosure | Contact email in privacy policy is a placeholder | accept | Placeholder `[UPDATE: your-email@example.com]` is used. Developer must fill before publishing. Flagged in checklist step 6 (Privacy & Data Safety). |

**ASVS L1 notes:** No authentication or cryptography in this plan. Privacy policy documentation supports ASVS V1 (Architecture) requirements by documenting data flows and third-party dependencies.
</threat_model>

<verification>
```bash
# Verify checklist exists
wc -l docs/RELEASE_CHECKLIST.md

# Verify asset spec exists
ls assets/store/screenshot_specs.md
grep "1920x1080" assets/store/screenshot_specs.md
grep "1024x500" assets/store/screenshot_specs.md

# Verify privacy policy
grep "AniList" assets/privacy_policy.md
grep "AnimeFire" assets/privacy_policy.md
grep "\-\-update" assets/privacy_policy.md

# Verify AGENTS.md updates
grep "## CI/CD" AGENTS.md
grep "RELEASE_CHECKLIST" AGENTS.md

# Verify existing content preserved
grep "## Build & Run" AGENTS.md
grep "## Testing" AGENTS.md
```
</verification>

<success_criteria>
- [x] `docs/RELEASE_CHECKLIST.md` exists with 9 sections and `- [ ]` checklist items covering full Play Store submission flow
- [x] `assets/store/screenshot_specs.md` documents all image requirements (1920×1080 screenshots, 1280×720 TV banner, 1024×500 feature graphic)
- [x] `assets/privacy_policy.md` covers AniList + scraper data collection, no data sharing, third-party services, children's privacy, contact placeholder
- [x] `AGENTS.md` updated with `## CI/CD` section and `RELEASE_CHECKLIST.md` reference
- [x] All existing AGENTS.md content preserved
</success_criteria>

<output>
Create `.planning/phases/05-release-configuration/05-03-SUMMARY.md` when done
</output>
